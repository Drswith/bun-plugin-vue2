{
  "name": "bun-vue2-playground",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --hot src/index.ts",
    "build": "bun build ./src/index.html --outdir=dist --sourcemap --target=browser --minify",
    "start": "NODE_ENV=production bun src/index.ts"
  },
  "dependencies": {
    "vue": "^2.7.16"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
